{
  "name": "bengal-international-university",
  "version": "1.0.0",
  "private": true,
  "description": "Bengal International University — public website + student/faculty/admin portal",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "init-db": "npm run init-db --prefix backend",
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
